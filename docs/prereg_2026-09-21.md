# Phase 5.3/5.4 pre-registration addendum — 2026-09-21
Nothing on the mix* grid has been read at the time of this commit.

## Authorising registry md5
62d1254829994ab8b2f6dbb6b2edd047 (registered_thresholds.json, 0 pending).
Controls-only verification: 16/16 thresholds recompute; only control_gpt2m and
control_gpt2m_greedyeval opened; eligible decoders ['topk', 'greedy'].

## Ordering deviation
Registration preceded this commit, the read_g1_g4.py fixes, and the two entries
below. No grid value had been read; all 16 thresholds reproduce at full precision.

## r = 25 seeds — no amendment
Grid stays n = 3 uniform (s43-45). Every r = 25 AUC, latency and jackknife result
is printed with its resolution (3 exposed vs 3 controls per decoder). Later r = 25
seeds are a replication under this frozen registry and cannot alter the verdict.

## Leave-one-out tau sensitivity — reported, never ranked
Every registered tau recomputed four times from the control arms, dropping one of
s43-46; the verdict table recomputed under each; the weakest reported beside the
registered verdict. A worse verdict is a stated limitation; a better one changes
nothing. LOO FPR is not reported (1/n by construction for a max rule).
